mybatis-plus:
  type-aliases-package: cc.allio.uno.turbo.**.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

spring:
  datasource:
    url: jdbc:postgresql://43.143.195.208:5432/turbo_test
    username: postgres
    password: allio@2022
    driver-class-name: org.postgresql.Driver

persistent:
  tenant:
    ignore-list:
      - student
      - org
      - biz_tree