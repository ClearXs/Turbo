turbo:
  ob:
    log:
      openobserve:
        # 是否启用日志
        enabled: false
        # 日志服务地址
        endpoint: http://localhost:5080
        # 组织
        # 日志头
        headers:
          Authorization: ""
          stream-name: "default"
        # 日志发送超时时间
        timeout: 100000
        organization: default
        stream: turbo

otel:
  instrumentation:
    logback-appender:
      enabled: true