turbo:
  oss:
    fetch: true
    provider: minio
    endpoint: ${OSS_ENDPOINT:http://localhost:8800}
    access-key: ${OSS_ACCESS_KEY:admin}
    secret-key: ${OSS_SECRET_KEY:admin}
    base-dir: /
    strategy: date
    bucket: ${OSS_BUCKET:turbo}
